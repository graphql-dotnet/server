image: Visual Studio 2017

version: '0.0.2-alpha-{build}'

build_script:  
  - ps: .\build.ps1 -Target "AppVeyor"

artifacts:
  - path: artifacts\*.nupkg
    name: Packages

branches:
  only:
    - develop
    - master
    - /bugs/.*/
    - /features/.*/