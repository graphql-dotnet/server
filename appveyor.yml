image: Visual Studio 2017

build_script:  
  - ps: .\build.ps1 -Target "Default"

artifacts:
  - path: artifacts\*.nupkg
    name: Packages

skip_branch_with_pr: true
branches:
  only:
    - develop
    - master
    - /bugs/.*/
    - /features/.*/
